# 阶段 12：重复启动保护

## 问题

项目服务已监听 `7860` 时再次运行 `start.bat`，第二个 Python 进程会因端口占用抛出 `Errno 10048`。

## 修复

1. `start.bat` 启动前检查本机 `7860` 监听状态。
2. 已存在服务时直接打开 `http://127.0.0.1:7860`。
3. 不再启动第二个 Python 进程。
4. 不终止现有服务，正在执行的图片任务不会受重复启动影响。
