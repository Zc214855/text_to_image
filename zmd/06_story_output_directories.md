# 阶段 6：按故事名保存输出

## 已完成

1. 每次生成结果保存到 `output/<故事名>/`。
2. 故事目录内统一保存 `prompts.json` 和 `scene_XX.png`。
3. 同名故事再次生成时使用 `<故事名>_2`、`<故事名>_3`，避免覆盖历史结果。
4. 自动替换 Windows 文件夹名中的非法字符。
5. 自动处理 `CON`、`NUL`、`COM1` 等 Windows 保留名称。
6. 故事名为空时使用 `未命名故事`。
7. 界面生成结果显示本次故事的完整保存路径。
