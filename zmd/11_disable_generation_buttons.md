# 阶段 11：生成期间禁用按钮

## 实现

1. 点击“生成插图”后立即禁用“生成插图”和“仅重试失败图片”。
2. 点击“仅重试失败图片”后同样立即禁用两个按钮。
3. 禁用操作不进入任务队列，优先更新页面状态。
4. 任务结束后恢复两个按钮。
5. 恢复事件不要求前置任务成功；正常返回或异常结束都会执行。
6. 按钮更新只修改 `interactive` 属性，不改变按钮文案和样式。

## 验证

- 27 项单元测试通过。
- 事件链检查确认生成和重试均为“禁用 -> 执行 -> 恢复”。
- 生成和重试仍共用 `image-generation` 单并发组。
