# 阶段 4：模型说明补全

## 已完成

1. 图像模型下拉框改为可读名称，并保留实际模型 ID 作为调用值。
2. 9 个已接入图像模型全部补充渠道、费用、定位和支持尺寸。
3. 新增当前模型动态详情卡，切换模型时同步更新说明和尺寸。
4. 下方模型表由 `ALL_MODELS` 自动生成，后续新增模型不会再漏写说明。
5. 明确当前执行链路：
   - 智谱 GLM-5.1 经腾讯云 LKEAP Token Plan 负责故事分析和分镜提示词。
   - 火山方舟 Seedream 5.0 Lite 负责最终图片生成。
6. 明确模型下拉框只切换图片模型，不切换分镜 LLM。
7. 标注万相 2.1 为仍可调用的旧版模型，百炼当前推荐万相 2.7 系列。
8. 标注 Qwen Image Plus 可评估升级到 Qwen Image 2.0。

## 验证

- 8 项单元测试通过。
- Gradio UI 构建通过。
- 浏览器确认 9 个下拉选项完整显示。
- 浏览器确认模型切换后详情卡和尺寸列表同步更新。
